class Hotel {
  int idHotel;
  String name;
  String primaryColor;
  String secondaryColor;
  String imageUrl;

  Hotel(this.idHotel, this.name, this.primaryColor, this.secondaryColor, this.imageUrl);
}